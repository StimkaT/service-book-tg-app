export default async function OtherPage() {

    return (
        <main className="text-gray-700">
            Пока пусто OtherPage (предполагается что это вкладка для записи какой-то не стандартной траты, где ты сам определяешь название и прочее)
        </main>
    );
}
